pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check
    clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings
    test:
      glob: "*.rs"
      run: cargo test --all-features
